name: voynich
description: A native cryptography plugin.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

  plugin:
    platforms:
      android:
        package: tabsvsspaces.voynich
        pluginClass: VoynichPlugin
      ios:
        pluginClass: VoynichPlugin